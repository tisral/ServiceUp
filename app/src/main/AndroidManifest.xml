<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ServiceUp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyProviders" />
        <activity android:name=".MyCustomers" />
        <activity android:name=".Map" />
        <activity android:name=".addToCart" />
        <activity android:name=".Dashboard" />
        <activity android:name=".ReviewAService" />
        <activity android:name=".ListAService" />
        <activity android:name=".Profile" />
        <activity android:name=".Login" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:theme="@style/Theme.MyApp" />

        <activity
            android:name=".CentralContainer"
            android:theme="@style/Theme.MyApp" />
        <activity
            android:name=".CreateAccountActivity"
            android:theme="@style/Theme.MyApp" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

</manifest>